{
  "pod": {
    "spec": {
      "volumes": [
        { 
          "persistentVolumeClaim": { "claimName": "nginx-data" }
        },
        {
          "persistentVolumeClaim": { "claimName": "nginx-config" }
        }
      ]
    }
  }
}